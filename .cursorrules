# Dual Wiki 研发与上游发布铁律 (Cursor Rules)

## 🚨 上游 PR 禁令（最高优先级铁律）

1. **绝对禁止自动向上游（`koreader/contrib` 或 `koreader/koreader`）发起任何 Pull Request（包括禁止调用 `gh pr create`）**！
2. 插件已通过 PR #176 和 #177 永久收录至官方目录。
3. **日常更新机制（工厂模式）**：
   - 任何常规功能新增、Bug 修复、UI 微调、多语言补全、小版本发布（如 `v1.3.1`、`v1.3.2` 等）：
   - **一律仅推送到个人仓库**（`YeYinYing/koreader-dual-wiki`），并在个人仓库打 Tag 与创建 Release（附带 `.zip` 压缩包）。
   - **禁止碰 `koreader/contrib`**。
4. **用户端更新原理**：
   - KOReader 的 `appstore.koplugin` 会直接轮询插件作者个人的 GitHub Release API。
   - 个人仓库一旦发布新 Release，全球用户的设备端即会自动亮起更新提示并一键直连下载更新，**完全无需上游官方介入**。
5. **何时才允许向上游提 PR？**
   - 只有在满足以下全部条件时才可提交：
     1. 发生重大跨代架构升级（如 `v2.0.0`）；
     2. **且**必须由人类用户显式输入确切口令（例如：“向官方提交 PR”或“发起上游 bump”）。
   - 未经用户明确授权，绝不执行任何跨仓库 PR 操作。
